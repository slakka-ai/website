const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');if(toggle&&nav){toggle.addEventListener('click',()=>nav.classList.toggle('open'));}const targets=[...document.querySelectorAll('.hero > .container > *, .section .section-head, .panel, .page-hero .container, .site-footer .footer-grid > *')];targets.forEach((el,i)=>{el.classList.add('reveal',i%2?'down':'up');});const io=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('is-visible');});},{threshold:.14});targets.forEach(el=>io.observe(el));